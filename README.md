* This is a assignment project of PH
* This is unfullfilled project
* I want to finish it later
# This is a [Next.js](https://nextjs.org/) project with tailwindcss with [`create-next-app`](https://github.com/vercel/next.js/tree/canary/packages/create-next-app).

## Getting Started
* open terminal
* run> npm i

then run the development server:

```bash
npm run dev
and
npm run json-server (for running the server)

Open [http://localhost:3000] and (http://localhost:5000) with your browser to see the result.
